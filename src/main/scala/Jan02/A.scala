package Jan02

import javax.naming.ldap.HasControls

object Main extends App {
  class A

  case class B()

  case object c
object D
  println(c.isInstanceOf[Serializable])
  println(D.isInstanceOf[Serializable])
}